custom_postgres:
  outputs:
    dev:
      type: postgres
      dbname: destination_db
      host: host.docker.internal
      pass: secret
      port: 5434
      schema: public
      threads: 1
      user: postgres
  target: dev
